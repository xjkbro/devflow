import styled from "styled-components";

export default function Hero() {
    return (
        <Container>
            <NavBar />
        </Container>
    );
}

const Container = styled.div`
    margin: 10px 200px;
`;
